<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Área do Cliente</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" />
</head>

<body>

    <!-- SIDEBAR -->
    <div class="sidebar" id="sidebar">
        <h4>👤 Cliente</h4>
        <hr />

        <ul class="nav flex-column">

            <li>
                <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "active" : "")"
                   asp-controller="AgendamentosClientes" asp-action="Index">
                    <i class="bi bi-list-check"></i>
                    <span class="link-text">Meus Agendamentos</span>
                </a>
            </li>

            <li>
                <a class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Create" ? "active" : "")"
                   asp-controller="AgendamentosClientes" asp-action="Create">
                    <i class="bi bi-plus-circle"></i>
                    <span class="link-text">Novo Agendamento</span>
                </a>
            </li>

        </ul>

        <div class="sidebar-footer">
            <span class="me-2">👤 @User.Identity?.Name</span>

            <form asp-controller="ClientesAuth" asp-action="login" method="post">
                <button type="submit" class="btn btn-sm btn-outline-danger w-100">Sair</button>
            </form>
        </div>
    </div>

    <!-- CONTEÚDO -->
    <div class="content" id="content">

        <!-- TOPBAR -->
        <div class="topbar">
            <span class="hamburger" id="hamburger">
                <i class="bi bi-list"></i>
            </span>
            <h5 class="mb-0">@ViewData["Title"]</h5>
        </div>

        <!-- BODY -->
        <div class="mt-5 p-4">
            @RenderBody()
        </div>

    </div>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        const sidebar = document.getElementById('sidebar');
        const hamburger = document.getElementById('hamburger');

        hamburger.addEventListener('click', () => {
            sidebar.classList.toggle('show');
        });
    </script>

    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>
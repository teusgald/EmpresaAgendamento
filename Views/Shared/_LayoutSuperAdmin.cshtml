@using Microsoft.AspNetCore.Identity
@using EmpresaAgendamento.Models
@inject UserManager<ApplicationUser> UserManager

@{
    var nomeUsuario = User.Identity?.Name;
}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Painel do Sistema</title>
    <link rel="icon" type="image/png" href="/img/icon.png?v=3" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>

    <!-- SIDEBAR -->
    <div class="sidebar" id="sidebar">
        <h4>🛡️ Dono do sistema</h4>
        <hr />
        <ul class="nav flex-column">

            <li class="nav-item">
                <a class="nav-link" asp-controller="SuperAdminDashboard" asp-action="Index">
                    <i class="bi bi-bar-chart"></i>
                    <span class="link-text">Dashboard</span>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link" asp-controller="SuperAdminFaturamento" asp-action="Index">
                    <i class="bi bi-cash-coin"></i>
                    <span class="link-text">Faturamento</span>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link" asp-controller="SuperAdminUsuarios" asp-action="Index">
                    <i class="bi bi-people"></i>
                    <span class="link-text">Usuários</span>
                </a>
            </li>

        </ul>

        <div class="sidebar-footer d-flex flex-column px-2 py-2">

            <span class="mb-2 fw-semibold text-white">
                Bem-vindo,<br> @nomeUsuario
            </span>

            <form method="post" asp-controller="SuperAdminAuth" asp-action="Logout" class="d-inline">
                @Html.AntiForgeryToken()
                <button type="submit" class="btn btn-link text-danger text-decoration-none p-0">
                    <i class="bi bi-box-arrow-right me-1"></i> Sair
                </button>
            </form>

        </div>
    </div>

    <!-- CONTEÚDO -->
    <div class="content" id="content">

        <div class="topbar">
            <span class="hamburger" id="hamburger"><i class="bi bi-list"></i></span>
            <span class="topbar-brand">Painel do Sistema</span>
        </div>

        <div class="p-4">
            @RenderBody()
        </div>

    </div>

    <div id="toast-container" class="toast-container"></div>

    @await RenderSectionAsync("Scripts", required: false)

    @if (TempData["ToastMessage"] != null)
    {
        <script>
            document.addEventListener("DOMContentLoaded", function () {
                showToast(
                    "@TempData["ToastMessage"]",
                    "@TempData["ToastType"]"
                );
            });
        </script>
    }

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const sidebar = document.getElementById('sidebar');
        const hamburger = document.getElementById('hamburger');
        const overlay = document.getElementById('overlay');

        if (hamburger) {
            hamburger.addEventListener('click', () => {
                sidebar.classList.toggle('show');

                if (overlay) {
                    overlay.classList.toggle('show');
                }
            });
        }

        if (overlay) {
            overlay.addEventListener('click', () => {
                sidebar.classList.remove('show');
                overlay.classList.remove('show');
            });
        }

        function showToast(message, type) {

            const container = document.getElementById("toast-container");

            if (!container) return;

            const toast = document.createElement("div");

            toast.style.position = "fixed";
            toast.style.top = "20px";
            toast.style.right = "20px";
            toast.style.zIndex = "999999";
            toast.style.minWidth = "300px";
            toast.style.padding = "15px";
            toast.style.borderRadius = "10px";
            toast.style.boxShadow = "0 4px 12px rgba(0,0,0,.15)";
            toast.style.color = "#fff";
            toast.style.fontWeight = "500";
            toast.style.animation = "slideIn 0.3s ease";

            if (type === "success") {
                toast.style.backgroundColor = "#198754";
                toast.innerHTML = "✅ " + message;
            }
            else if (type === "error") {
                toast.style.backgroundColor = "#dc3545";
                toast.innerHTML = "❌ " + message;
            }
            else {
                toast.style.backgroundColor = "#ffc107";
                toast.style.color = "#000";
                toast.innerHTML = "⚠️ " + message;
            }

            container.appendChild(toast);

            setTimeout(() => {
                toast.style.opacity = "0";

                setTimeout(() => {
                    toast.remove();
                }, 300);
            }, 3000);
        }
    </script>
</body>
</html>
